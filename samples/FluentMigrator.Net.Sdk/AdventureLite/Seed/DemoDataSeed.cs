namespace AdventureLite.Seed
{
    // Implements FluentMigrator's ISeed when the package reference is
    // present; conformance is enforced by the FluentMigrator analyzer.
    public class DemoDataSeed
    {
        public int Order { get { return 1; } }
    }
}
